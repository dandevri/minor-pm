<!DOCTYPE html>
<html lang="nl">
  <head>
    <% include head.ejs %>
  </head>
  <body>
    <% include header.ejs %>

    <% standings.forEach(function(standing) { %>
      <h2><%= standing.position %></h2>
    <% }) %>

  </body>
</html>
